import { styled, Typography } from "@mui/material";

export const WeekDayLabel = styled(Typography)({
    width: 40,
    textAlign: 'center',
    color: '#757575',
    fontSize: 14,
    fontWeight: 500,
});