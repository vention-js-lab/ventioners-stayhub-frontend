export const ENDPOINTS = {
  root: '/',
  categories: '/categories',
  amenities: '/amenities',
  payments: '/payments',
  reviews: '/reviews',
} as const;

export const ROUTES = {
  root: '/',
} as const;
