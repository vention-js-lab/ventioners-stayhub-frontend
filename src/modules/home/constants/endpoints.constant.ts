export const ENDPOINTS = {
  root: '/',
  accommodations: '/accommodations',
  categories: '/categories',
  wishlist: '/users/wishlist',
  amenities: '/amenities',
  payments: '/payments',
} as const;

export const ROUTES = {
  root: '/',
} as const;
