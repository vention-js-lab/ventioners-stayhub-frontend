import { type MuiStylesObject } from '#/types/mui-styles-object.type';

export const ownerPropertiesRouteStyles = {
  container: { display: 'flex', flexDirection: 'column', width: '100%' },
} satisfies MuiStylesObject;
